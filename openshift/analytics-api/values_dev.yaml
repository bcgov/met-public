environment: dev

image:
  tag: latest
  namespace: e903c2-tools

cors:
  origins: 'https://dep-analytics-api-dev.apps.gold.devops.gov.bc.ca'

route:
  host: dep-analytics-api-dev.apps.gold.devops.gov.bc.ca
  path: /api/
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge

vault:
  engine: 'e903c2-nonprod'
  path: '/dev'