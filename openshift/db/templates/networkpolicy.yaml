kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/instance: met-patroni
      app.kubernetes.io/name: patroni
      cluster-name: met-patroni
  policyTypes:
  - Ingress
  ingress:
  - from:
    {{- range .Values.db.allowIngressPodselectors }}
    - podSelector:
        matchLabels:
{{ toYaml . | indent 10 }}
    {{- end }}
