environment: dev

site:
  url: met-web-dev.apps.gold.devops.gov.bc.ca

keycloak:
  baseUrl: 'dev.loginproxy.gov.bc.ca'

email:
  notificationsEndpoint: 'https://met-notify-api-dev.apps.gold.devops.gov.bc.ca/api/v1/notifications/email'
  environment: 'You are using the DEV environment'

image:
  tag: dev

vault:
  engine: 'e903c2-nonprod'
  path: '/dev'