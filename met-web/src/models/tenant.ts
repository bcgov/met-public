export interface Tenant {
    name?: string;
    short_name: string;
    contact_name: string;
    contact_email: string;
    title: string;
    description?: string;
    logo_url?: string;
    logo_credit?: string;
    logo_description?: string;
}
